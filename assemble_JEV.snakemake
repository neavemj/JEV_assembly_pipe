
"""
JEV Genome Assembly Pipeline
Pipeline for assembling raw Illumina reads into
Japanese encephalitis whole genomes

File locations and options can be changed in the config.yaml file
"""

import os, sys

configfile: "config.yaml"

rules_dir = os.path.join(os.path.expanduser(config["program_dir"]), "rules")

include: os.path.join(rules_dir, "preprocessing.smk")
include: os.path.join(rules_dir, "assembly.smk")


rule all:
    input:
        "report.html"
